# Lab02 — QC-LDPC Decoder

A hardware decoder for quasi-cyclic low-density parity-check (QC-LDPC)
codes, exploring the trade-offs among circuit area, timing and decoding latency.

## Release status

The implementation, architecture notes and measured results will be
published after the assignment deadline.

> The decoder can fix bit errors. It can't fast-forward the deadline.
